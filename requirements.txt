# Core dependencies for Vercel deployment
# Using Pydantic v1 for Vercel Python runtime compatibility
# Using httpx directly for Supabase REST API (no supabase package needed)
fastapi==0.99.1
uvicorn[standard]==0.23.2
python-dotenv==1.0.1
httpx==0.27.0
pydantic==1.10.14
